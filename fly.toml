# fly.toml
app = "Nexus"
primary_region = "nrt"  # 東京リージョン

[build]

[env]
  NODE_ENV = "production"

# Discord BotはHTTPサーバー不要なので、最小構成
[http_service]
  # Bot単体ならHTTPサービスは不要 → コメントアウトのまま

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
